<h1><%= @user.username %></h1>

<% if @current_user.id == @user.id %>
<%= link_to 'Edit profile', edit_user_path %>
<% end %>

<p><%= @user.id %></p>

<% @beets.each do |beet| %>

<% if beet.user_id == @user.id %>

  <div class="post">
  <pre><%= AsciiArt.new("#{ beet.image }").to_ascii_art(width: 70) %></pre>
  <div class="info">
  <a href="/users/<%= beet.user_id %>"><%= beet.user.username %></a>
  <p>♡ 103</p>
  <a href="/beets/<%= beet.id %>">comments</a>
  </div>
  </div>

<% end %>

<% end %>
